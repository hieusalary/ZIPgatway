Tôi sẽ vẽ lại toàn bộ gói tin từ khi nhận được cho đến khi đến application layer, dựa trên các đoạn code đã trace.

## 📦 **PACKET JOURNEY: RAW → APPLICATION**

Dựa trên code trace từ các file:
- uip6.c - UDP processing
- tcpip.c - TCP/IP stack
- ZW_udp_server.c - ZIP packet parsing
- ZIP_Router.c - Application handling

---

## 🎯 **STAGE 0: RAW ETHERNET FRAME (từ network driver)**

```
┌────────────────────────────────────────────────────────────────────────────┐
│ ETHERNET FRAME (1500 bytes total)                                         │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│ ┌──────────────────────────────────────────────────────────────────────┐ │
│ │ ETHERNET HEADER (14 bytes)                                           │ │
│ ├──────────────────────────────────────────────────────────────────────┤ │
│ │ Destination MAC: 6 bytes  | 00:11:22:33:44:55                       │ │
│ │ Source MAC:      6 bytes  | AA:BB:CC:DD:EE:FF                       │ │
│ │ EtherType:       2 bytes  | 0x86DD (IPv6)                           │ │
│ └──────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
│ ┌──────────────────────────────────────────────────────────────────────┐ │
│ │ IPv6 HEADER (40 bytes)                                               │ │
│ ├──────────────────────────────────────────────────────────────────────┤ │
│ │ Version/TC/FL:   4 bytes  | 0x60000000                              │ │
│ │ Payload Length:  2 bytes  | 0x0050 (80 bytes)                       │ │
│ │ Next Header:     1 byte   | 0x11 (UDP)                              │ │
│ │ Hop Limit:       1 byte   | 0x40 (64)                               │ │
│ │ Source IP:      16 bytes  | fd00:aaaa::1                            │ │
│ │ Dest IP:        16 bytes  | fd00:bbbb::1                            │ │
│ └──────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
│ ┌──────────────────────────────────────────────────────────────────────┐ │
│ │ UDP HEADER (8 bytes)                                                 │ │
│ ├──────────────────────────────────────────────────────────────────────┤ │
│ │ Source Port:     2 bytes  | 0x101B (4123 - Z-Wave port)            │ │
│ │ Dest Port:       2 bytes  | 0x101B (4123)                           │ │
│ │ Length:          2 bytes  | 0x0050 (80 bytes total = header+data)  │ │
│ │ Checksum:        2 bytes  | 0xABCD                                  │ │
│ └──────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
│ ┌──────────────────────────────────────────────────────────────────────┐ │
│ │ ZIP PACKET (Z-Wave over IP) (72 bytes)                              │ │
│ ├──────────────────────────────────────────────────────────────────────┤ │
│ │ Command Class:   1 byte   | 0x23 (COMMAND_CLASS_ZIP)               │ │
│ │ Command:         1 byte   | 0x02 (COMMAND_ZIP_PACKET)              │ │
│ │ Flags0:          1 byte   | 0x80 (ACK_REQ set)                     │ │
│ │ Flags1:          1 byte   | 0x41 (ZW_CMD_INCL | HDR_EXT_INCL)     │ │
│ │ SeqNo:           1 byte   | 0x05                                    │ │
│ │ Source EP:       1 byte   | 0x00                                    │ │
│ │ Dest EP:         1 byte   | 0x00                                    │ │
│ │ ┌────────────────────────────────────────────────────────────────┐ │ │
│ │ │ HEADER EXTENSIONS (5 bytes)                                    │ │ │
│ │ ├────────────────────────────────────────────────────────────────┤ │ │
│ │ │ Extension Length: 1 byte | 0x05                                │ │ │
│ │ │ Extension Type:   1 byte | 0x82 (EFI - Encap Format Info)     │ │ │
│ │ │ Extension Len:    1 byte | 0x02                                │ │ │
│ │ │ Security Level:   1 byte | 0x02 (S2_AUTHENTICATED)             │ │ │
│ │ │ Security Type:    1 byte | 0x00                                │ │ │
│ │ └────────────────────────────────────────────────────────────────┘ │ │
│ │ ┌────────────────────────────────────────────────────────────────┐ │ │
│ │ │ Z-WAVE COMMAND (59 bytes - encrypted if S2)                    │ │ │
│ │ ├────────────────────────────────────────────────────────────────┤ │ │
│ │ │ Command Class:   1 byte  | 0x20 (COMMAND_CLASS_BASIC)          │ │ │
│ │ │ Command:         1 byte  | 0x01 (BASIC_SET)                    │ │ │
│ │ │ Value:           1 byte  | 0xFF (ON)                           │ │ │
│ │ │ ... (more data)  56 bytes                                      │ │ │
│ │ └────────────────────────────────────────────────────────────────┘ │ │
│ └──────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘

Buffer: uip_buf[UIP_BUFSIZE]
Total size: 14 + 40 + 8 + 72 = 134 bytes
uip_len = 134
```

**Key variables tại thời điểm này**:
```c
uip_len = 134                    // Total packet size
uip_buf[0..13] = Ethernet header // Layer 2
uip_buf[14..53] = IPv6 header   // Layer 3 (UIP_LLH_LEN = 14)
uip_buf[54..61] = UDP header    // Layer 4
uip_buf[62..133] = UDP payload  // ZIP + Z-Wave command
```

---

## 🎯 **STAGE 1: SAU KHI tcpip_input() - Loại bỏ Ethernet header**

**File**: `contiki/core/net/tcpip.c:229`

```c
static void packet_input(void)
{
  if(uip_len > 0) {
    uip_input();  // ← Call uip_process(UIP_DATA)
  }
}
```

**Packet structure KHÔNG đổi**, nhưng pointer adjustment:

```
┌────────────────────────────────────────────────────────────────────────────┐
│ AFTER tcpip_input() - uip_buf still contains full frame                   │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│ uip_buf[0..13]:   Ethernet header (IGNORED by uIP)                        │
│                   │                                                        │
│                   ▼                                                        │
│ ┌──────────────────────────────────────────────────────────────────────┐ │
│ │ IPv6 HEADER (40 bytes) - uip_buf[UIP_LLH_LEN..UIP_LLH_LEN+39]       │ │
│ ├──────────────────────────────────────────────────────────────────────┤ │
│ │ uip_buf[14..53]                                                      │ │
│ │   UIP_IP_BUF = (struct uip_ip_hdr *)&uip_buf[UIP_LLH_LEN]          │ │
│ │                                                                      │ │
│ │ Version/TC/FL:   4 bytes  | 0x60000000                              │ │
│ │ Payload Length:  2 bytes  | 0x0050 (80 bytes)                       │ │
│ │ Next Header:     1 byte   | 0x11 (UDP)                              │ │
│ │ Hop Limit:       1 byte   | 0x40                                    │ │
│ │ Source IP:      16 bytes  | fd00:aaaa::1                            │ │
│ │ Dest IP:        16 bytes  | fd00:bbbb::1                            │ │
│ └──────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
│ ┌──────────────────────────────────────────────────────────────────────┐ │
│ │ UDP HEADER (8 bytes) - uip_buf[54..61]                               │ │
│ ├──────────────────────────────────────────────────────────────────────┤ │
│ │ UIP_UDP_BUF = (struct uip_udp_hdr *)&uip_buf[uip_l2_l3_hdr_len]    │ │
│ │   where uip_l2_l3_hdr_len = UIP_LLH_LEN + UIP_IPH_LEN = 14 + 40    │ │
│ │                                                                      │ │
│ │ Source Port:     2 bytes  | 0x101B (4123)                           │ │
│ │ Dest Port:       2 bytes  | 0x101B (4123)                           │ │
│ │ Length:          2 bytes  | 0x0050 (80)                             │ │
│ │ Checksum:        2 bytes  | 0xABCD                                  │ │
│ └──────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
│ ┌──────────────────────────────────────────────────────────────────────┐ │
│ │ UDP PAYLOAD (72 bytes) - uip_buf[62..133]                            │ │
│ │ ZIP Packet + Z-Wave command (chưa parse)                             │ │
│ └──────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘

uip_len = 134 (unchanged)
```

---

## 🎯 **STAGE 2: TRONG uip_process() - SAU UDP HEADER REMOVAL**

**File**: `contiki/core/net/uip6.c:1473`

```c
udp_input:
  // STEP 1: Remove UDP + IP headers
  uip_len = uip_len - UIP_IPUDPH_LEN;  // ← Subtract 48 bytes (40 IPv6 + 8 UDP)
  
  // STEP 2: Point to payload
  uip_appdata = &uip_buf[UIP_LLH_LEN + UIP_IPUDPH_LEN];
  //                      └─ 14        └─ 48
  //                      = uip_buf[62] ← ZIP packet starts here
```

**Packet structure SAU KHI loại bỏ headers**:

```
┌────────────────────────────────────────────────────────────────────────────┐
│ AFTER HEADER REMOVAL in uip_process()                                     │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│ uip_buf[0..61]: Headers (STILL IN BUFFER but IGNORED)                     │
│                                                                            │
│                 ┌─── uip_appdata points here                              │
│                 ▼                                                          │
│ ┌──────────────────────────────────────────────────────────────────────┐ │
│ │ APPLICATION DATA (72 bytes) - uip_buf[62..133]                       │ │
│ ├──────────────────────────────────────────────────────────────────────┤ │
│ │ uip_appdata = &uip_buf[UIP_LLH_LEN + UIP_IPUDPH_LEN]                │ │
│ │             = &uip_buf[62]                                           │ │
│ │                                                                      │ │
│ │ ┌────────────────────────────────────────────────────────────────┐ │ │
│ │ │ ZIP PACKET HEADER (7 bytes)                                    │ │ │
│ │ ├────────────────────────────────────────────────────────────────┤ │ │
│ │ │ [0] Command Class: 0x23 (COMMAND_CLASS_ZIP)                    │ │ │
│ │ │ [1] Command:       0x02 (COMMAND_ZIP_PACKET)                   │ │ │
│ │ │ [2] Flags0:        0x80 (ACK_REQ)                              │ │ │
│ │ │ [3] Flags1:        0x41 (ZW_CMD | HDR_EXT)                     │ │ │
│ │ │ [4] SeqNo:         0x05                                        │ │ │
│ │ │ [5] Source EP:     0x00                                        │ │ │
│ │ │ [6] Dest EP:       0x00                                        │ │ │
│ │ └────────────────────────────────────────────────────────────────┘ │ │
│ │ ┌────────────────────────────────────────────────────────────────┐ │ │
│ │ │ HEADER EXTENSIONS (5 bytes)                                    │ │ │
│ │ ├────────────────────────────────────────────────────────────────┤ │ │
│ │ │ [7]  Ext Length:   0x05                                        │ │ │
│ │ │ [8]  Ext Type:     0x82 (EFI)                                  │ │ │
│ │ │ [9]  Ext Len:      0x02                                        │ │ │
│ │ │ [10] Sec Level:    0x02 (S2_AUTH)                              │ │ │
│ │ │ [11] Sec Type:     0x00                                        │ │ │
│ │ └────────────────────────────────────────────────────────────────┘ │ │
│ │ ┌────────────────────────────────────────────────────────────────┐ │ │
│ │ │ Z-WAVE COMMAND (60 bytes)                                      │ │ │
│ │ ├────────────────────────────────────────────────────────────────┤ │ │
│ │ │ [12] CC:    0x20 (BASIC)                                       │ │ │
│ │ │ [13] Cmd:   0x01 (BASIC_SET)                                   │ │ │
│ │ │ [14] Value: 0xFF                                               │ │ │
│ │ │ [15..71] More data...                                          │ │ │
│ │ └────────────────────────────────────────────────────────────────┘ │ │
│ └──────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘

uip_len = 72               // ← Đã trừ đi 48 bytes (IPv6 + UDP headers)
uip_datalen() = 72         // ← Same as uip_len
uip_appdata = &uip_buf[62] // ← Points to ZIP packet
```

**Key changes**:
- `uip_len`: 134 → 72 (trừ 48 bytes headers)
- `uip_appdata`: Points to byte 62 (ZIP packet start)
- Headers vẫn còn trong buffer nhưng bị IGNORE

---

## 🎯 **STAGE 3: tcpip_uipcall() → process_post_synch()**

**File**: `contiki/core/net/tcpip.c:1036`

```c
void tcpip_uipcall(void)
{
  struct uip_udp_conn_appstate *ts = &uip_udp_conn->appstate;
  
  if(ts->p != NULL) {
    // Post event to registered process (udp_server_process)
    process_post_synch(ts->p, tcpip_event, ts->state);
    //                  ^^^^^  ^^^^^^^^^^^
    //                  Process  Event ID
  }
}
```

**Packet KHÔNG thay đổi**, nhưng event được post:

```
┌────────────────────────────────────────────────────────────────────────────┐
│ Event Posted to udp_server_process                                        │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│ process_post_synch(udp_server_process, tcpip_event, &server_conn)         │
│                                                                            │
│ Data still in uip_buf:                                                     │
│   uip_appdata = &uip_buf[62]   (ZIP packet)                               │
│   uip_len = 72                 (ZIP packet + Z-Wave command)              │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

---

## 🎯 **STAGE 4: UDPCommandHandler() - Parse ZIP Header**

**File**: `src/ZW_udp_server.c:884`

```c
void UDPCommandHandler(struct uip_udp_conn* c, const u8_t* data, u16_t len)
{
  ZW_COMMAND_ZIP_PACKET* pZipPacket = (ZW_COMMAND_ZIP_PACKET*) data;
  //                                                            ^^^^
  //                                                            = uip_appdata
  
  // Parse ZIP header
  ses.seq = pZipPacket->seqNo;                    // data[4]
  ses.lendpoint = pZipPacket->dEndpoint;          // data[6]
  ses.rendpoint = pZipPacket->sEndpoint;          // data[5]
  
  // Parse header extensions
  uint8_t* payload = &pZipPacket->payload[0];     // data[7]
  uint8_t hdr_ext_len = *payload++;               // data[7] = 0x05
  
  // Skip extensions
  udp_payload_len = len - ZIP_HEADER_SIZE - hdr_ext_len;
  //                = 72 - 7 - 5 = 60 bytes
  
  payload += hdr_ext_len;  // payload now points to Z-Wave command
  //                          = data[12]
}
```

**Packet structure SAU KHI parse ZIP header**:

```
┌────────────────────────────────────────────────────────────────────────────┐
│ AFTER ZIP HEADER PARSING                                                   │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│ ┌──────────────────────────────────────────────────────────────────────┐ │
│ │ ZIP PACKET STRUCTURE (72 bytes total)                                │ │
│ ├──────────────────────────────────────────────────────────────────────┤ │
│ │                                                                      │ │
│ │ [0..6]   ZIP Header (7 bytes) ← PARSED, saved to ses struct         │ │
│ │          ses.seq = 0x05                                             │ │
│ │          ses.lendpoint = 0x00                                       │ │
│ │          ses.rendpoint = 0x00                                       │ │
│ │                                                                      │ │
│ │ [7..11]  Header Extensions (5 bytes) ← PARSED                       │ │
│ │          scheme = efi_to_scheme(0x02, 0x00)                         │ │
│ │                 = SECURITY_SCHEME_2_AUTHENTICATED                   │ │
│ │                                                                      │ │
│ │          ┌─── payload points here                                   │ │
│ │          ▼                                                           │ │
│ │ [12..71] Z-WAVE COMMAND (60 bytes) ← Will be queued                 │ │
│ │          ┌────────────────────────────────────────────────────────┐ │ │
│ │          │ [0] Command Class: 0x20 (BASIC)                        │ │ │
│ │          │ [1] Command:       0x01 (BASIC_SET)                    │ │ │
│ │          │ [2] Value:         0xFF                                │ │ │
│ │          │ [3..59] More data                                      │ │ │
│ │          └────────────────────────────────────────────────────────┘ │ │
│ └──────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
│ Variables:                                                                 │
│   udp_payload_len = 60                                                     │
│   payload = &data[12]                                                      │
│   scheme = SECURITY_SCHEME_2_AUTHENTICATED                                 │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

**ZIP header đã được parse và lưu vào struct**:
```c
zwave_udp_session_t ses = {
  .seq = 0x05,
  .lendpoint = 0x00,  // Dest endpoint (Gateway)
  .rendpoint = 0x00,  // Source endpoint (Node)
  .conn = *c          // UDP connection info
};

security_scheme_t scheme = SECURITY_SCHEME_2_AUTHENTICATED;
```

---

## 🎯 **STAGE 5: Queued to async_queue**

**File**: `src/ZW_udp_server.c:1055`

```c
p = uip_packetqueue_alloc(&async_queue,
    (u8_t*) payload - sizeof(struct async_state),  // ← Include metadata
    sizeof(struct async_state) + udp_payload_len,  // ← Total size
    1000);  // Timeout

s = (struct async_state*) p->queue_buf;
s->conn.rendpoint = pZipPacket->sEndpoint;  // 0x00
s->conn.lendpoint = pZipPacket->dEndpoint;  // 0x00
s->conn.scheme = scheme;  // SECURITY_SCHEME_2_AUTHENTICATED
s->ack_req = pZipPacket->flags0 & ZIP_PACKET_FLAGS0_ACK_REQ;  // TRUE

// Copy Z-Wave command after metadata
memcpy(s + 1, payload, udp_payload_len);
```

**Packet structure TRONG QUEUE**:

```
┌────────────────────────────────────────────────────────────────────────────┐
│ ASYNC_QUEUE ENTRY                                                          │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│ ┌──────────────────────────────────────────────────────────────────────┐ │
│ │ METADATA (struct async_state)                                        │ │
│ ├──────────────────────────────────────────────────────────────────────┤ │
│ │ ack_req:           TRUE                                              │ │
│ │ conn.rendpoint:    0x00                                              │ │
│ │ conn.lendpoint:    0x00                                              │ │
│ │ conn.scheme:       SECURITY_SCHEME_2_AUTHENTICATED                   │ │
│ │ conn.conn:         UDP connection (src/dst IP, ports)                │ │
│ │ conn.rx_flags:     0                                                 │ │
│ │ conn.seq:          0x05                                              │ │
│ └──────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
│ ┌──────────────────────────────────────────────────────────────────────┐ │
│ │ Z-WAVE COMMAND PAYLOAD (60 bytes)                                    │ │
│ ├──────────────────────────────────────────────────────────────────────┤ │
│ │ [0] Command Class:  0x20 (COMMAND_CLASS_BASIC)                       │ │
│ │ [1] Command:        0x01 (BASIC_SET)                                 │ │
│ │ [2] Value:          0xFF (ON)                                        │ │
│ │ [3..59] Additional data                                              │ │
│ └──────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
│ Total queue entry size: sizeof(async_state) + 60                          │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

---

## 🎯 **STAGE 6: do_app_handler() - Send ZIP ACK + Dispatch**

**File**: `src/ZW_udp_server.c:821`

```c
static void do_app_handler() {
  while (uip_packetqueue_buflen(&async_queue)) {
    s = (struct async_state*) uip_packetqueue_buf(&async_queue);
    udp_payload_len = uip_packetqueue_buflen(&async_queue) - sizeof(struct async_state);
    //                                                          = 60 bytes
    
    // Send ZIP ACK if needed
    if (s->ack_req) {
      send_udp_ack(&s->conn, RES_ACK);  // ← ZIP ACK frame sent back
    }
    
    // Call application handler with PURE Z-Wave command
    ApplicationIpCommandHandler(&s->conn,
        (BYTE*) s + sizeof(struct async_state),  // ← Skip metadata
        udp_payload_len);                         // ← 60 bytes
  }
}
```

**ZIP ACK frame sent back** (separate packet):

```
┌────────────────────────────────────────────────────────────────────────────┐
│ ZIP ACK PACKET (Sent back to source)                                      │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│ [Ethernet Header] (14 bytes)                                               │
│ [IPv6 Header]     (40 bytes) - Src/Dst swapped                            │
│ [UDP Header]      (8 bytes)  - Ports swapped                              │
│                                                                            │
│ ┌──────────────────────────────────────────────────────────────────────┐ │
│ │ ZIP ACK PAYLOAD (8 bytes)                                            │ │
│ ├──────────────────────────────────────────────────────────────────────┤ │
│ │ [0] Command Class: 0x23 (COMMAND_CLASS_ZIP)                          │ │
│ │ [1] Command:       0x02 (COMMAND_ZIP_PACKET)                         │ │
│ │ [2] Flags0:        0x08 (ACK_RES)                                    │ │
│ │ [3] Flags1:        0x00                                              │ │
│ │ [4] SeqNo:         0x05 (same as received)                           │ │
│ │ [5] Dest EP:       0x00 (original source)                            │ │
│ │ [6] Source EP:     0x00 (original dest)                              │ │
│ │ [7] Reserved:      0x00                                              │ │
│ └──────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

**Data passed to ApplicationIpCommandHandler**:

```
┌────────────────────────────────────────────────────────────────────────────┐
│ DATA TO ApplicationIpCommandHandler()                                      │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│ Connection info (zwave_connection_t):                                      │
│   - rendpoint: 0x00                                                        │
│   - lendpoint: 0x00                                                        │
│   - scheme: SECURITY_SCHEME_2_AUTHENTICATED                                │
│   - ripaddr: fd00:aaaa::1                                                  │
│   - lipaddr: fd00:bbbb::1                                                  │
│   - rport: 4123                                                            │
│   - lport: 4123                                                            │
│                                                                            │
│ ┌──────────────────────────────────────────────────────────────────────┐ │
│ │ PURE Z-WAVE COMMAND (60 bytes)                                       │ │
│ ├──────────────────────────────────────────────────────────────────────┤ │
│ │ [0] Command Class:  0x20 (COMMAND_CLASS_BASIC)                       │ │
│ │ [1] Command:        0x01 (BASIC_SET)                                 │ │
│ │ [2] Value:          0xFF                                             │ │
│ │ [3..59] Additional data                                              │ │
│ └──────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
│ All ZIP headers REMOVED - Only Z-Wave command remains!                    │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

---

## 🎯 **STAGE 7: ApplicationIpCommandHandler() - Command Class Routing**

**File**: `src/ZW_ZIPApplication.c:404`

```c
bool ApplicationIpCommandHandler(zwave_connection_t *c, void *pData, u16_t bDatalen)
{
  ZW_APPLICATION_TX_BUFFER* pCmd = (ZW_APPLICATION_TX_BUFFER*)pData;
  
  // Route to command handler
  switch(ZW_command_handler_run(c, pData, bDatalen, FALSE)) {
    case COMMAND_HANDLED:
      return true;
    // ... other cases
  }
}
```

**File**: `src/command_handler.c:83`

```c
command_handler_codes_t ZW_command_handler_run(zwave_connection_t *connection,
                                                uint8_t* payload, 
                                                uint16_t len)
{
  // Validate command
  validator_result_t result = ZW_command_validator(payload, len, &version);
  
  // Find handler
  for (fn = __start__handlers; fn < __stop__handlers; fn++) {
    if (fn->cmdClass == payload[0]) {  // ← payload[0] = 0x20 (BASIC)
      if (supports_frame_at_security_level(fn, connection->scheme)) {
        return fn->handler(connection, payload, len);
        //     ^^^^^^^^^^
        //     Basic_CommandHandler(connection, payload, len)
      }
    }
  }
}
```

**Final dispatch to Command Class handler**:

```
┌────────────────────────────────────────────────────────────────────────────┐
│ DISPATCH TO COMMAND CLASS HANDLER                                         │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│ Basic_CommandHandler(connection, payload, len)                             │
│                       ^^^^^^^^^^  ^^^^^^^  ^^^                             │
│                       |           |        60 bytes                        │
│                       |           Points to Z-Wave command                 │
│                       Connection metadata                                  │
│                                                                            │
│ ┌──────────────────────────────────────────────────────────────────────┐ │
│ │ COMMAND STRUCTURE                                                    │ │
│ ├──────────────────────────────────────────────────────────────────────┤ │
│ │ payload[0]:  0x20  (COMMAND_CLASS_BASIC)                             │ │
│ │ payload[1]:  0x01  (BASIC_SET)                                       │ │
│ │ payload[2]:  0xFF  (Value = ON)                                      │ │
│ │ payload[3..59]: Additional data                                      │ │
│ └──────────────────────────────────────────────────────────────────────┘ │
│                                                                            │
│ Command handler will:                                                      │
│   1. Validate command format                                               │
│   2. Check security level (S2_AUTHENTICATED is OK)                         │
│   3. Execute action (turn device ON)                                       │
│   4. Send response (if needed)                                             │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

---

## 📊 **SUMMARY TABLE: PACKET SIZE CHANGES**

| Stage | Location | Total Size | Headers Removed | Payload Size | Notes |
|-------|----------|------------|-----------------|--------------|-------|
| **0** | Raw Ethernet | 134 bytes | - | 134 | Full frame from network |
| **1** | tcpip_input() | 134 bytes | 0 | 134 | Ethernet ignored but in buffer |
| **2** | uip_process() | 72 bytes | 48 bytes | 72 | IPv6 (40) + UDP (8) removed |
| **3** | tcpip_event | 72 bytes | 0 | 72 | Event posted, data unchanged |
| **4** | UDPCommandHandler | 60 bytes | 12 bytes | 60 | ZIP header (7) + Ext (5) parsed |
| **5** | async_queue | 60 bytes | 0 | 60 | + metadata struct added |
| **6** | do_app_handler | 60 bytes | 0 | 60 | Metadata stripped before dispatch |
| **7** | CC Handler | 60 bytes | 0 | 60 | Pure Z-Wave command |

---

## 🎯 **KEY BYTE POSITIONS**

```
Original buffer (uip_buf[]):
[0..13]:   Ethernet header (14 bytes)          ← IGNORED by uIP
[14..53]:  IPv6 header (40 bytes)              ← Removed at uip_process()
[54..61]:  UDP header (8 bytes)                ← Removed at uip_process()
[62..68]:  ZIP header (7 bytes)                ← Parsed by UDPCommandHandler
[69..73]:  ZIP extensions (5 bytes)            ← Parsed by UDPCommandHandler
[74..133]: Z-Wave command (60 bytes)           ← Sent to application

After all processing:
Command Class handler receives ONLY:
[0..59]:   Pure Z-Wave command (60 bytes)
```

Đây là visualization hoàn chỉnh của packet journey từ raw bytes đến application handler!
